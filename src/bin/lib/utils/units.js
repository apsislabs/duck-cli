export const insToPts = (s = 0) => s * 72;
const ptsToIns = (s = 0) => s / 72;
const insToPx = (s = 0) => s * 300;
const pxToIns = (s = 0) => s / 300;
export const pxToPts = (s = 0) => (s / 300) * 72;
const ptsToPx = (s = 0) => (s / 72) * 300;
